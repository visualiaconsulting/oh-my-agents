# Bug Fix Report: ConfigInvalidError (2026-05-21)

## Descripción del Problema
Se reportó un error de tipo `ConfigInvalidError` al intentar cargar la configuración de agentes en **opencode**. El error impedía el inicio o la carga correcta del sistema debido a un esquema de configuración inválido.

## Error Log
```json
{
  "body": {
    "name": "ConfigInvalidError",
    "data": {
      "path": "...\\.opencode\\agents\\qa_reviewer.md",
      "issues": [
        {
          "path": ["mode"],
          "message": "Expected \"subagent\" | \"primary\" | \"all\", got \"secondary\""
        }
      ]
    }
  },
  "status": 400
}
```

## Causa Raíz
Varios archivos de definición de agentes (`.md`) contenían el campo `mode: secondary`. Sin embargo, el esquema de validación de **opencode** solo acepta los siguientes valores para el campo `mode`:
- `primary`: Para agentes orquestadores principales.
- `subagent`: Para agentes especializados que reciben tareas delegadas.
- `all`: Para agentes con capacidades mixtas.

El valor `secondary` no es una opción válida en la versión actual del motor de opencode.

## Archivos Afectados y Corregidos
Se identificaron y corrigieron los siguientes archivos en `C:\Users\ekrde\OneDrive\ML2025\Investigacion\agentes\.opencode\agents\`:

1.  `qa_reviewer.md` (`secondary` -> `subagent`)
2.  `prompt_crafter.md` (`secondary` -> `subagent`)
3.  `proptech_expert.md` (`secondary` -> `subagent`)
4.  `python_architect.md` (`secondary` -> `subagent`)

## Resolución
Se realizó una sustitución quirúrgica del término `mode: secondary` por `mode: subagent` en todos los archivos afectados para alinearlos con el esquema esperado.

## Verificación Final
Se realizó un `grep_search` en el directorio de agentes confirmando que todos los archivos ahora cumplen con los valores permitidos (`primary` o `subagent`).
